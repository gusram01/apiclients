// import { Pool } from 'pg';

// if (process.env.NODE_ENV !== 'production') {
//   require('dotenv').config();
// }
// const connectionString = process.env.POSTG1 as string;
// const pool = new Pool({ connectionString });

// const db = async (constrain: any) => {
//   return {
//     query: (text: string) => {
//       pool.query(text, constrain);
//     },
//   };
// };

// export default db;
